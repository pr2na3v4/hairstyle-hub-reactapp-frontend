:root {
  --primary-dark: #312e2f;
  --secondary-dark: #2b2828;
  --text-light: #e4dada;
  --bg-light: #f9f9f9;
  --shadow: 0 4px 15px rgba(0,0,0,0.05);
  --modal-bg: rgba(0, 0, 0, 0.4);
}

/* 1. Profile Layout */
.profile-container {
  max-width: 800px;
  margin: 0 auto;
  padding: 20px;
}

.profile-card {
  text-align: center;
  background: white;
  padding: 30px;
  border-radius: 20px;
  box-shadow: var(--shadow);
  margin-bottom: 30px;
}

.profile-avatar {
  width: 100px;
  height: 100px;
  border-radius: 50%;
  cursor: zoom-in;
  object-fit: cover;
  margin-bottom: 15px;
  border: 3px solid var(--primary-dark);
}

/* 2. Buttons */
.profile-btns {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-top: 15px;
}

.edit-btn, .logout-btn {
  padding: 10px 24px;
  border-radius: 25px;
  border: 1px solid #ddd;
  cursor: pointer;
  font-weight: 600;
  transition: 0.2s;
}

.edit-btn:hover { background: #f0f0f0; }
.logout-btn { background: #fafafa; color: #181515; border: none; }

/* 3. User Activity Sections */
.user-section { margin-top: 40px; }
.user-section h3 { 
  margin-bottom: 20px; 
  border-left: 4px solid var(--primary-dark); 
  padding-left: 10px; 
}

.profile-comment-item {
  background: var(--bg-light);
  padding: 15px;
  border-radius: 12px;
  margin-bottom: 10px;
  position: relative;
}

.delete-btn {
  color: #4b4647;
  background: none;
  border: none;
  cursor: pointer;
  font-size: 13px;
  font-weight: 600;
}

/* 4. Modal / Edit Profile Popup */
.modal-overlay {
  position: fixed;
  top: 0; left: 0;
  width: 100%; height: 100%;
  background: var(--modal-bg);
  backdrop-filter: blur(5px);
  display: flex; align-items: center; justify-content: center;
  z-index: 3000;
  padding: 20px;
}

.modal-content {
  background: white; /* Changed from dark to white for better readability */
  width: 100%;
  max-width: 400px;
  padding: 30px;
  border-radius: 24px;
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
  text-align: center;
  animation: slideUp 0.3s ease-out;
}

.modal-content h3 { color: var(--primary-dark); margin-bottom: 20px; }

.modal-content input[type="text"] {
  width: 100%;
  padding: 14px;
  margin-bottom: 15px;
  border: 1.5px solid #eee;
  border-radius: 12px;
  outline: none;
}

/* 5. Custom File Upload Button */
.custom-file-upload {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  padding: 12px;
  background: var(--bg-light);
  color: var(--primary-dark);
  border: 1.5px dashed #ccc;
  border-radius: 12px;
  cursor: pointer;
  font-weight: 600;
  width: 100%;
  margin-bottom: 15px;
}

.preview-img {
  width: 80px; height: 80px;
  border-radius: 50%;
  object-fit: cover;
  margin: 10px auto 20px;
  display: block;
  border: 2px solid var(--primary-dark);
}

/* 6. Image Lightbox (FULL SCREEN FIX) */
.image-lightbox {
  position: fixed;
  top: 0; left: 0;
  width: 100%; height: 100%; /* Full viewport */
  background: rgba(0, 0, 0, 0.95);
  display: none; 
  align-items: center; justify-content: center;
  z-index: 9999; /* Above everything */
}

.image-lightbox.active { display: flex; }

.enlarged-img {
  max-width: 90%;
  max-height: 85%;
  object-fit: contain;
  border-radius: 8px;
  animation: zoomIn 0.3s ease-out;
}

/* Animations */
@keyframes slideUp {
  from { transform: translateY(20px); opacity: 0; }
  to { transform: translateY(0); opacity: 1; }
}

@keyframes zoomIn {
  from { transform: scale(0.9); opacity: 0; }
  to { transform: scale(1); opacity: 1; }
}